customtkinter>=5.2.0
Pillow>=10.0.0
qrcode[pil]>=7.4
reportlab>=4.0.0

# Windows only — RAW print spooling (strongly recommended)
# pip install pywin32
pywin32>=306; sys_platform == "win32"

# RFID hardware (optional — app runs in simulation without it)
# pip install RFIDIOt
